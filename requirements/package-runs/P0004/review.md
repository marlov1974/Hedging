# P0004 consistency review

Result: PASS.

P0004 is a documentation package. The existing currency component document treats currency as a separate money-based component and avoids real rates, organizations or proprietary terms.

Implementation note: the package document was moved from the repository root to `docs/components/` now that P0001 has created the documentation structure. The package file was moved to `requirements/packages/`.

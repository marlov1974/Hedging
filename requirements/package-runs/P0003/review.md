# P0003 consistency review

Result: PASS.

P0003 is a documentation package. The existing call-off, transaction and customer transaction list documents use generic terms and cover the required parent-child model, customer-visible instrument name and monthly component transaction concepts.

Implementation note: the package documents were moved from the repository root to `docs/data-model/` now that P0001 has created the documentation structure. The package file was moved to `requirements/packages/`.

# P0005 consistency review

Result: PASS.

P0005 is a documentation package. The existing market derivation, Q-factor and runtime derivation policy documents use generic terms and distinguish customer transactions, source transaction parameters, Q-factor, derived Market quantity and derived Market value.

Implementation note: the package documents were moved from the repository root to `docs/market-derivation/` now that P0001 has created the documentation structure. The package file was moved to `requirements/packages/`.

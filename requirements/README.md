# Requirements

Requirements are organized as ordered packages.

Package files live in `requirements/packages/`.
Package-run evidence lives in `requirements/package-runs/`.

All requirement text must use synthetic examples and neutral terminology.

## Current implementation handover

[P0038 — Local resource hybrid prototype](packages/P0038-resource-hybrid-local-prototype.md)
is ready for Codex to implement in the existing application on the Mac.
It covers B2C events, a ten-resource advisor call-off, Modern/Classic reports,
customer and market legs, and q-factor changes. Status: requirements only.
